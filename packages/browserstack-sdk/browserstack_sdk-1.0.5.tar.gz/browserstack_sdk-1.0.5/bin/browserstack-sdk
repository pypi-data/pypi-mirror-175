#!/usr/bin/env python

import sys
import browserstack_sdk

NO_ARGS_ERROR = 'Expected at least 1 input, received 0'

if __name__ == '__main__':
  if len(sys.argv) <= 1:
    print(NO_ARGS_ERROR)
  else:
    browserstack_sdk.run_on_browserstack(sys.argv)
