# do not import all endpoints into this module because that uses a lot of memory and stack frames
# if you need the ability to import all endpoints from this module, import them with
# from collibra_core.paths.mappings_external_system_external_system_id_removal_jobs import Api

from collibra_core.paths import PathValues

path = PathValues.MAPPINGS_EXTERNAL_SYSTEM_EXTERNAL_SYSTEM_ID_REMOVAL_JOBS