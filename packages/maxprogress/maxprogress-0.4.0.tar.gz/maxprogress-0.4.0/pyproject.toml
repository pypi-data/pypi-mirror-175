[tool.poetry]
name = "maxprogress"
version = "0.4.0"
description = "A customized Rich Progress Bar"
authors = ["maxludden <dev@maxludden.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
rich = "^12.6.0"
maxconsole = "^0.4.0"

[tool.poetry.scripts]
maxprogress = "maxprogress:main"
get_progress = "maxprogress:get_progress"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
