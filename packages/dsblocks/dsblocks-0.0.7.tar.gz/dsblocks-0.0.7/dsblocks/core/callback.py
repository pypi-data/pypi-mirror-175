# AUTOGENERATED! DO NOT EDIT! File to edit: ../../nbs/core/callback.ipynb.

# %% auto 0
__all__ = ['BaseCallback']

# %% ../../nbs/core/callback.ipynb 3
from ..config import bt_defaults as dflt
from ..utils.utils import replace_attr_and_store

# %% ../../nbs/core/callback.ipynb 6
class BaseCallback ():
    def __init__ (self, but='', **kwargs):
        from dsblocks.core.components import Component
        parent = replace_attr_and_store (base_class=BaseCallback, replace_generic_attr=False, 
                                         return_parent=True, but=but)
        if isinstance (parent, Component):
            self.parent = parent
            if not hasattr(self.parent, 'callbacks'):
                self.parent.callbacks = []
            self.parent.callbacks += [self]
