from setuptools import setup

setup(name='distributions_fitting_v001',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_fitting_v001'],
      zip_safe=False)
