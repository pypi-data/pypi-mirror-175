from setuptools import setup

setup(name='probab_stats_distributions',
      version='0.2',
      description='Gaussian & Binomial distributions',
      packages=['probab_stats_distributions'],
      zip_safe=False)
