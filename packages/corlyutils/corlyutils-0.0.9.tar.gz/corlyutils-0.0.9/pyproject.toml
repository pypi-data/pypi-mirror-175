[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "corlyutils"
version = "0.0.9"
authors = [
    { name = "Example Author", email = "author@example.com" },
]
description = "A small example package"
readme = "README"
license = { file = "LICENSE" }
requires-python = ">=3.7"
dependencies = ['requests', 'pymongo', 'mysqlclient']