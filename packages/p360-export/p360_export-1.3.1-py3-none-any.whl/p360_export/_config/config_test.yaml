parameters:
  databricksbundle:
    databricks_connect:
      connection:
        token:
        address:

  p360_export:
    config:
      secret_getter: 'env_variable'
      dbutils_secrets_scope: 'unit-kv'
      azure_storage_config_base_path: 'abfss://configs@odapczlakeg2dev.dfs.core.windows.net'
      local_file_config_base_path: '.'
      config_getter: 'local_file'